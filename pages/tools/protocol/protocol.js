const PROTOCOLS = [
  {
    key: 'uart',
    name: 'UART',
    subtitle: '异步串口 · 全双工 · 点对点',
    pins: [
      { pin: 'TX', desc: '数据发送端，接对方 RX（交叉连接）' },
      { pin: 'RX', desc: '数据接收端，接对方 TX（交叉连接）' },
      { pin: 'GND', desc: '公共地，必须连接' }
    ],
    params: [
      { k: '常见波特率', v: '9600 / 19200 / 115200 / 921600' },
      { k: '数据格式', v: '1 起始位 + 8 数据位 + 无校验 + 1 停止位（最常用）' },
      { k: '电平标准', v: 'TTL 3.3V/5V；RS-232 用 MAX3232 转换；远距离用 RS-485' },
      { k: '空闲电平', v: '高电平；起始位为低电平' }
    ],
    tips: [
      '双方波特率、数据位、停止位、校验位必须一致',
      'TTL 电平不可直接接 RS-232/RS-485 设备，需电平转换',
      '无时钟线，靠约定波特率采样，高波特率下注意线长',
      '传输 1 字节实际占 10 位（含起始/停止位）'
    ]
  },
  {
    key: 'spi',
    name: 'SPI',
    subtitle: '同步串行 · 全双工 · 一主多从',
    pins: [
      { pin: 'SCLK', desc: '时钟，由主机产生' },
      { pin: 'MOSI', desc: '主出从入（主机发送）' },
      { pin: 'MISO', desc: '主入从出（主机接收）' },
      { pin: 'CS/SS', desc: '片选，低电平选中从机' }
    ],
    params: [
      { k: '四种模式', v: 'Mode 0~3，由 CPOL（时钟极性）+ CPHA（时钟相位）组合' },
      { k: '常见速率', v: '1~50MHz，受从设备规格限制' },
      { k: '数据顺序', v: '多数器件 MSB 先发，注意查手册' }
    ],
    tips: [
      '主机和从机 SPI 模式（CPOL/CPHA）必须匹配',
      '多从机共用三根总线，每个从机独立一根 CS 线',
      '先拉低 CS，稍延时后再发时钟；通信结束先拉高 CS',
      '无应答机制，读写时序严格按芯片手册指令表'
    ]
  },
  {
    key: 'i2c',
    name: 'I2C',
    subtitle: '同步串行 · 半双工 · 两线多设备',
    pins: [
      { pin: 'SDA', desc: '数据线，开漏输出，需上拉电阻' },
      { pin: 'SCL', desc: '时钟线，开漏输出，需上拉电阻' }
    ],
    params: [
      { k: '上拉电阻', v: '典型 4.7kΩ（100kHz）/ 2.2kΩ（400kHz）' },
      { k: '寻址方式', v: '7 位地址（同总线最多约 112 个设备），也有 10 位扩展' },
      { k: '标准速率', v: '标准 100k / 快速 400k / 快速+ 1M / 高速 3.4M' }
    ],
    tips: [
      '起始条件：SCL 高电平时 SDA 由高变低；停止条件相反',
      '每传输 8 位后第 9 个时钟为 ACK 应答位',
      '总线空闲时 SDA/SCL 都保持高电平',
      '地址通常 7 位，注意手册给出的地址是否已含读写位（8 位写法）'
    ]
  },
  {
    key: 'can',
    name: 'CAN',
    subtitle: '差分总线 · 多主仲裁 · 抗干扰强',
    pins: [
      { pin: 'CAN_H', desc: '差分正线（经 CAN 收发器）' },
      { pin: 'CAN_L', desc: '差分负线（经 CAN 收发器）' },
      { pin: '120Ω', desc: '终端电阻，总线两端各接一个' }
    ],
    params: [
      { k: '常见速率', v: '125k / 250k / 500k / 1M bps' },
      { k: '电平特征', v: '显性 0（CAN_H−CAN_L ≈ 2V）/ 隐性 1（差压 ≈ 0V）' },
      { k: '帧 ID', v: '标准帧 11 位 / 扩展帧 29 位，数据段 0~8 字节' }
    ],
    tips: [
      'MCU 的 CAN 外设需经收发器（如 TJA1050/SN65HVD230）接总线',
      '总线两端各并一个 120Ω 终端电阻，整线总阻约 60Ω',
      'ID 越小优先级越高，仲裁由硬件自动完成',
      '适合汽车/工业场景，节点数与线长受波特率约束'
    ]
  }
];

Page({
  data: {
    active: 'uart',
    list: PROTOCOLS
  },

  switchProto(e) {
    this.setData({ active: e.currentTarget.dataset.key });
  }
});
